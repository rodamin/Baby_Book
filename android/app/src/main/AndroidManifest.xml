<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.dsm2016.baby_book">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity" />
        <activity android:name=".SplashActivity">
            <!--<intent-filter>-->
                <!--<action android:name="android.intent.action.MAIN" />-->

                <!--<category android:name="android.intent.category.LAUNCHER" />-->
            <!--</intent-filter>-->
        </activity>
        <activity android:name=".LoginActivity" />
        <activity android:name=".BaseActivity" />
        <activity android:name=".DiariesActivity"/>
        <activity android:name=".SignupActivity"/>
        <activity android:name=".BabyInfoActivity"/>
        <activity android:name=".CalendarActivity" />
        <activity android:name=".GraphActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".CoupleStoryActivity" />
        <activity android:name=".MarryStoryActivity" />
        <activity android:name=".BabyStoryActivity" />
        <activity android:name=".PregnantStoryActivity" />
        <activity android:name=".MyDiaryActivity"/>
    </application>

</manifest>